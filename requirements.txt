# Core AI/ML
tensorflow==2.10.1  # Patched version of 2.10.0
keras==2.10.0       # Must match TF version
protobuf==3.20.3    # Critical for serialization

# Numeric Computing
numpy==1.23.5       # Last version supporting TF 2.10
scipy==1.9.3        # Compatible with numpy 1.23.x
pandas==1.5.3       # Stable with this stack

# Image Processing
Pillow==9.5.0       # Security-patched version
opencv-python-headless==4.7.0.72  # Modern but compatible

# Visualization
plotly==5.13.1      # Stable with pandas 1.5.x
matplotlib==3.7.1   # Backward-compatible
seaborn==0.12.2     # Matches matplotlib

# Streamlit Ecosystem
streamlit==1.22.0
streamlit-aggrid==0.3.4.post3  # Patched version

# Utilities
python-dotenv==1.0.0
tqdm==4.65.0        # Modern with back-compat
h5py==3.8.0         # Works despite TF docs (tested)
